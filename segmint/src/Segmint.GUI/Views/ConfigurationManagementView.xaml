<UserControl x:Class="Segmint.GUI.Views.ConfigurationManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:viewmodels="clr-namespace:Segmint.GUI.ViewModels"
             Background="{DynamicResource SystemControlBackgroundAltHighBrush}">

    <UserControl.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="MinWidth" Value="120"/>
        </Style>
        
        <Style TargetType="TextBox" BasedOn="{StaticResource DefaultTextBoxStyle}">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource SystemControlBackgroundAccentBrush}" 
               CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Configuration Management" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Manage vendor-specific HL7 configurations and patterns" 
                              FontSize="14" Foreground="White" Opacity="0.9" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="📁 Create New" Style="{DynamicResource AccentButtonStyle}" 
                           Command="{Binding CreateConfigurationCommand}" Margin="5"/>
                    <Button Content="🔄 Refresh" Command="{Binding LoadConfigurationsCommand}" Margin="5"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Filter Controls -->
        <Border Grid.Row="1" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}" 
               CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                        ui:ControlHelper.PlaceholderText="Search configurations..." 
                        ui:ControlHelper.Header="Search"/>

                <ComboBox Grid.Column="1" ItemsSource="{Binding VendorFilters}" 
                         SelectedItem="{Binding SelectedVendorFilter}"
                         ui:ControlHelper.Header="Vendor"/>

                <ComboBox Grid.Column="2" ItemsSource="{Binding MessageTypeFilters}" 
                         SelectedItem="{Binding SelectedMessageTypeFilter}"
                         ui:ControlHelper.Header="Message Type"/>

                <TextBlock Grid.Column="3" Text="{Binding StatusMessage}" VerticalAlignment="Center" 
                          FontStyle="Italic" Margin="15,0,0,0"/>
            </Grid>
        </Border>

        <!-- Loading Indicator -->
        <ui:ProgressBar Grid.Row="2" IsIndeterminate="True" Height="4" Margin="0,0,0,10"
                       Visibility="{Binding IsLoading, Converter={x:Static ui:Converters.BooleanToVisibilityConverter}}"/>

        <!-- Configuration List -->
        <Border Grid.Row="3" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}" 
               CornerRadius="8">
            <Grid>
                <DataGrid ItemsSource="{Binding FilteredConfigurations}" 
                         SelectedItem="{Binding SelectedConfiguration}"
                         AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                         GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                         RowBackground="{DynamicResource SystemControlBackgroundAltHighBrush}"
                         AlternatingRowBackground="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Vendor" Binding="{Binding Vendor}" Width="150" MinWidth="100"/>
                        <DataGridTextColumn Header="Message Type" Binding="{Binding MessageType}" Width="120" MinWidth="100"/>
                        <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="80" MinWidth="60"/>
                        <DataGridTextColumn Header="Created" Binding="{Binding CreatedText}" Width="100" MinWidth="80"/>
                        <DataGridTextColumn Header="Confidence" Binding="{Binding ConfidenceText}" Width="90" MinWidth="70"/>
                        <DataGridTextColumn Header="Details" Binding="{Binding DetailsText}" Width="*" MinWidth="200"/>
                        
                        <DataGridTemplateColumn Header="Actions" Width="200" MinWidth="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="5">
                                        <Button Content="✅" ToolTip="Validate" Width="30" Height="30" Margin="2"
                                               Command="{Binding DataContext.ValidateConfigurationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}"/>
                                        <Button Content="📤" ToolTip="Export" Width="30" Height="30" Margin="2"
                                               Command="{Binding DataContext.ExportConfigurationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}"/>
                                        <Button Content="🗑" ToolTip="Delete" Width="30" Height="30" Margin="2"
                                               Command="{Binding DataContext.DeleteConfigurationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}" 
                                               Style="{DynamicResource DeleteButtonStyle}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Empty State -->
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" 
                           Visibility="{Binding FilteredConfigurations.Count, Converter={x:Static ui:Converters.BooleanToVisibilityConverter}, ConverterParameter=True}">
                    <TextBlock Text="📁" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                    <TextBlock Text="No configurations found" FontSize="18" HorizontalAlignment="Center" Margin="0,10"/>
                    <TextBlock Text="Create a new configuration by analyzing HL7 sample messages" 
                              FontSize="14" HorizontalAlignment="Center" Opacity="0.7" Margin="0,5"/>
                    <Button Content="Create Your First Configuration" 
                           Style="{DynamicResource AccentButtonStyle}" 
                           Command="{Binding CreateConfigurationCommand}"
                           Margin="0,15"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Quick Actions Panel -->
        <Border Grid.Row="4" Background="{DynamicResource SystemControlBackgroundAltHighBrush}" 
               CornerRadius="8" Padding="15" Margin="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="💡 Quick Actions:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,15,0"/>
                    <Button Content="📥 Import Configuration" Margin="5"/>
                    <Button Content="🔗 Link Configurations" Margin="5"/>
                    <Button Content="📊 View Statistics" Margin="5"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding FilteredConfigurations.Count, StringFormat='Showing {0} configurations'}" 
                              VerticalAlignment="Center" FontSize="12" Opacity="0.7"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>